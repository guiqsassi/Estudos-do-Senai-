let lista = []
let verifica

    for(i=0;i<10;i++){
        lista.push(Number(prompt("me fala 10 números pra por na lista")))
    }

verifica = Number(prompt("qual número quer verificar se existe?"))

    if(lista[0] == verifica){
        alert("o número digitado está presente no primeiro vetor")
    }
    if(lista[])